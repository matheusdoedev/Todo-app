import styled from "styled-components";

import bgDesktopDarkTheme from "../../assets/images/bg-desktop-dark.jpg";

export const HeaderWrapper = styled.header`
  background: no-repeat url(${bgDesktopDarkTheme});
  height: 300px;

  padding: var(--gap-lg) 0;
`;
